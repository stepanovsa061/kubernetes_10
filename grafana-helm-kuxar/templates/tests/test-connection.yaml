apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "grafana-helm-kuxar.fullname" . }}-test-connection"
  labels:
    {{- include "grafana-helm-kuxar.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "grafana-helm-kuxar.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
